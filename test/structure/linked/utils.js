const src = '../../../src/structure/linked'
const { expect } = require('chai')
const { Node, Linked, Stack, Queue, Bag } = require(src)

module.exports = {
  src,
  expect,
  Node,
  Linked,
  Stack,
  Queue,
  Bag
}
